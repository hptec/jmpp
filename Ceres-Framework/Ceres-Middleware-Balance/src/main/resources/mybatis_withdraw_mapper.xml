<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.cerestech.middleware.balance.mapper.WithdrawMapper">
	
	<select id="search" resultType="cn.cerestech.middleware.balance.entity.Withdraw">
		SELECT * FROM `$$balance_withdraw` bw 
		WHERE TRUE
		<if test="owner_id != null">
			AND bw.owner_id = #{owner_id}
		</if>
		<if test="owner_type != null">
			AND bw.owner_type = #{owner_type}
		</if>
		<if test="balance_id != null">
			AND bw.balance_id = #{balance_id}
		</if>
		ORDER BY bw.create_time DESC
		<if test="offset != null and pageSize != null">
			LIMIT #{offset},#{pageSize}
		</if>
	</select>
</mapper>